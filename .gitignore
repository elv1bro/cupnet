node_modules
dist
out
*.log
log
.*/
.roomodes
!.github

# Playwright / local e2e output (not for VCS)
test-results/

# Bundled Node for AzureTLS worker (from scripts/ensure-cupnet-node.mjs before electron-builder)
resources/cupnet-node/
resources/.cupnet-node-tmp/

# Local debug dumps (HTML/JSON captures, gitleaks noise, IVAC, stability baselines — not for VCS)
_debug/
_debug2/
_debug*.zip

# Secrets — шаблоны с плейсхолдерами остаются в репозитории
.env
.env.local
.env.*.local

# Локальные полные zip-бэкапы рабочей копии (не коммитить)
cupnet2-workspace-backup-*.zip
cupnet-pre-rewrite.bundle
