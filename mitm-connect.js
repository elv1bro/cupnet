'use strict';

/**
 * CONNECT / TLS upgrade helpers for the MITM proxy.
 * Core logic remains in mitm-proxy.js; this module is reserved for further splits
 * (see CupNet architecture notes) and may re-export internal helpers in the future.
 */

module.exports = {
    // Placeholder for CONNECT pipeline extraction (mitm-proxy.js).
};
